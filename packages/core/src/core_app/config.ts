import { themeVariables } from '../theme';

const themeConfig = {
  components: {
    Table: {
      headerBg: themeVariables.color.theme02,
      headerBorderRadius: 0,
    },
  },
};

export { themeConfig };
