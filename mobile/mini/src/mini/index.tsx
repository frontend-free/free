import { env } from './env';
import { validator } from './validator';
import { route } from './route';
import { transformToPx } from './px';

const Mini = {
  env,
  validator,
  route,
  transformToPx,
};

export { Mini };
